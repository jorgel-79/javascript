var paso;
var paso2;

for (paso = 1; paso <=10; paso++) {
    //se ejecuta 10 veces , con valores desde paso 0 a paso 9.
    console.log(`este es el paso ${paso}`);
};

for (paso2 = 10; paso2 > 0; paso2--) {
    //se ejecuta 10 veces , con valores desde paso 10 a paso 0.
    console.log(`este es el paso ${paso2}`);
};